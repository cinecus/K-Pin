const { name, age, isStudent, balance, fruits, person, calculateTotal, promise } = require("./0-data-type");
const { calculateGrade, calculateDay } = require("./1-condition");
const { sayHello, add, subtract, logAnswer } = require("./2-function");


